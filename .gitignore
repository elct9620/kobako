/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/
*.bundle
*.so
*.o
*.a
mkmf.log
target/

# Build-time toolchain downloads (wasi-sdk, mruby tarballs); never committed.
/vendor/

# Pre-built Wasm guest binary. Generated by `rake compile` from wasm/ sources;
# bundled into the released gem at publish time, but not tracked in git.
/data/kobako.wasm

# rbs collection install destination. Regenerable from rbs_collection.lock.yaml
# via `bundle exec rbs collection install`; the lockfile is committed.
/.gem_rbs_collection/
