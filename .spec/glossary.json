{
  "glossary": [
    {
      "include": [
        "SPEC.md",
        "README.md",
        "docs/**/*.md",
        "lib/**/*.rb",
        "ext/**/*.rs",
        "crates/**/*.rs",
        "wasm/**/*.rs",
        "examples/**/*.rb",
        "examples/**/*.md",
        ".spec/glossary.json"
      ],
      "terms": [
        {
          "term": "Host App",
          "definition": "The application that embeds kobako. It holds every credential and policy decision, and chooses which of its own objects the guest may reach.",
          "not": []
        },
        {
          "term": "Host Gem",
          "definition": "kobako itself — the side of the boundary that owns the guest, routes what it asks for, and decides what each outcome means.",
          "not": []
        },
        {
          "term": "Guest Binary",
          "definition": "The compiled artifact untrusted code runs inside. It is the isolation boundary: nothing crosses it except as a message.",
          "not": []
        },
        {
          "term": "Service",
          "definition": "A host object the guest reaches by name. It is the only route from guest code to a host resource.",
          "not": [
            {
              "term": "adapter",
              "reason": "Names a translation role. What distinguishes a Service is that the guest can name it at all."
            }
          ]
        },
        {
          "term": "Wire Spec",
          "definition": "The contract every message crossing the boundary answers to. Each side implements it independently, so it is an agreement rather than a shared component.",
          "not": []
        },
        {
          "term": "Transport",
          "definition": "The exchange of messages across the boundary. One Call is answered by one Reply, and both directions use that same pair.",
          "not": []
        },
        {
          "term": "Envelope",
          "definition": "The part of a message that says where it goes and how it turned out. It is readable without a Codec, so routing and attribution never depend on one.",
          "not": []
        },
        {
          "term": "Codec",
          "definition": "The agreement two endpoints reach about how values become bytes. It is replaceable: only the two endpoints need to share one.",
          "not": []
        },
        {
          "term": "Outcome",
          "definition": "The final result of one Invocation. Every Invocation writes exactly one, whether it succeeded or failed.",
          "not": []
        },
        {
          "term": "Panic",
          "definition": "The failed arm of an Outcome. It attributes the failure to a side of the boundary and carries the Error Record describing it.",
          "not": []
        },
        {
          "term": "Fault",
          "definition": "The reason a Call is refused, returned to whoever issued it. It is kobako's own data rather than the caller's, so it rides the Envelope.",
          "not": []
        },
        {
          "term": "Error Record",
          "definition": "A failure's name, message, and backtrace as one unit. Every channel that reports a guest failure carries this same shape.",
          "not": []
        },
        {
          "term": "Run",
          "definition": "An Invocation that names an entrypoint already loaded in the guest, instead of supplying source to execute.",
          "not": []
        },
        {
          "term": "Frame",
          "definition": "Setup data handed to the guest before an Invocation begins, so the guest starts knowing what this run was configured with.",
          "not": []
        },
        {
          "term": "Catalog",
          "definition": "What a Sandbox has registered — the bindings and preloads fixed at setup, together with the Handle table each Invocation mints for itself.",
          "not": []
        },
        {
          "term": "Invocation",
          "definition": "One run of guest code, from entry until it settles. It is the act; the Execution is the record it leaves.",
          "not": []
        },
        {
          "term": "Execution",
          "definition": "The record one Invocation leaves — what it produced, what it wrote, and what it consumed. Frozen once the Invocation settles, and never revised.",
          "not": []
        }
      ]
    }
  ]
}
